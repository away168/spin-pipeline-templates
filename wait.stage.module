{
  "name": "{{ var "name" ?: "custom wait" }}",
  "refId": {{ var "refID" ?: "101" }},
  "requisiteStageRefIds": {{ var "requisiteStageRefIds" ?: [] }}
  "type": "wait",
  "waitTime": {{ var "waitTime" ?: 10 }}
}
